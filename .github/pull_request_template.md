## Summary

<!-- What changed and why (English). -->

## Changes

<!-- Bullet list of concrete edits. -->

## Validation

<!-- e.g. npm run lint, npm run build, manual checks. -->

## Out of scope

<!-- Explicit non-goals. -->

---

## Maintainer checklist

- [ ] **If this PR targets `main`:** confirm `docs/coordination/` is **not** in the merge diff (remove with `git rm -r docs/coordination` on the merging branch if needed). Per-version sync Markdown must stay on `v*` branches only — see [docs/coordination/README.md](../docs/coordination/README.md).
