# Frontend sync — v1.5.0

**Branch:** `feat/access-requests-portal-spa-crud-completion` (ADO, parallel) then `feat/access-requests-portal-spa-export-and-import` (after BFF deploy)  
**Last updated (UTC):** 2026-05-27  
**Author / agent:** backend handoff (for the front-end agent)

---

## Goal for this release line

Wire the SPA to the **full BFF CRUD surface** and deliver stakeholder outputs:
**Excel/CSV download** (BFF) and **PDF** (browser print).

---

## Depends on backend

- [x] OpenAPI: `/api/v1/openapi.json` on deployed App (regenerate after merge).
- [x] Contract: [`docs/FRONTEND_API_CONTRACT.md`](../FRONTEND_API_CONTRACT.md) §11.
- [ ] ADO branch `feat/access-requests-portal-export-and-crud-completion` merged/deployed to SIT.

---

## Frontend work planned

### Wave A — can start now (endpoints already on `master`)

- [ ] `FiltersBar` — `status`, `requester_email`, `opened_from`/`opened_to`; persist in URL.
- [ ] Pagination — `limit`/`offset`, **Load more** (no total count from API).
- [ ] `TicketDrawer` tabs — **Details | Edit | Audit**.
- [ ] Edit — `patch` + `row_version`; `409` → refetch `get` + user message.
- [ ] Delete — confirm modal → `remove(id, { rowVersion })`.
- [ ] Events — `useTicketEvents` + timeline (`before` cursor).
- [ ] Bulk update — row checkboxes + toolbar → `bulkUpdate` + per-row badges.
- [ ] Print PDF — `@media print` + **Print** button (`window.print()`).
- [ ] `openapi-typescript` script; re-export types from generated file.

### Wave B — after BFF export/bulk-create is live

- [ ] `HttpTicketsRepository.export()` — blob + `Content-Disposition` filename.
- [ ] Export modal — format, column checkboxes, filters.
- [ ] Import modal — CSV/XLSX parse → `bulkCreate` + per-row results.

---

## Blockers / questions for backend

- None for Wave A.
- Wave B blocked until SIT serves `GET /tickets/export` and `POST /tickets:bulk-create`.

---

## Out of scope (explicit)

- Server-side PDF.
- Dashboard page (sidebar stays `soon`).
- Legacy GitHub `src/` tree — do not extend.
