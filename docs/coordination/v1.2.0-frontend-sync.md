# Frontend sync — v1.2.0

**Branch:** `v1.2.0`  
**Last updated (UTC):** 2026-05-26  
**Author / agent:** (fill in)

---

## Goal for this release line

- Hold frontend modernization until backend + Excel contract is defined.
- Prepare for swapping `localStorage` persistence behind a `TicketsRepository` interface once API exists.

---

## Depends on backend

- [ ] OpenAPI / shared types location: _TBD_
- [ ] Auth: _TBD_ (delegated Graph vs BFF-only tokens)
- [ ] Confirm JSON field names align with [`src/types.ts`](../../src/types.ts) or document migration

---

## Frontend work planned (this branch)

- _None yet — coordination stub._ Update as soon as backend publishes the first contract draft.

---

## Blockers / questions for backend

- Where will the authoritative workbook or API live?
- Expected concurrency model for Excel rows?

---

## Out of scope (explicit)

- No deep refactor of [`src/components/SheetDatabase.tsx`](../../src/components/SheetDatabase.tsx) until read/write contract is stable.
